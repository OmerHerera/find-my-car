# Default: browser-local mock data, with no backend or database required.
NEXT_PUBLIC_DATA_MODE=mock

# Set to api to use the integrated Next.js route handlers and PostgreSQL.
# NEXT_PUBLIC_DATA_MODE=api
# DATABASE_URL=postgresql://user:password@host:5432/find_my_car?sslmode=require